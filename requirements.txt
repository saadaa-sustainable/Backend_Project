fastapi==0.115.0
uvicorn[standard]==0.32.0
httpx==0.27.2
sqlalchemy==2.0.35
greenlet==3.5.4
asyncpg==0.29.0
psycopg2-binary==2.9.12
alembic==1.13.3
pydantic==2.9.2
pydantic-settings==2.5.2
apscheduler==3.10.4
python-dotenv==1.0.1
tenacity==9.0.0
orjson==3.10.7
structlog==24.4.0
python-pptx==1.0.2
python-multipart==0.0.12
anthropic==0.120.2

# Google APIs.
# google-cloud-bigquery was MISSING, and that is why the `bq_inventory`
# and `silver_returns` steps have failed on every nightly run
# (ModuleNotFoundError: google.cloud.bigquery) -- not a credentials
# problem, as it looked. google-api-python-client reads the graphics
# requisition sheet in scripts/ingest_asset_sources.py.
google-cloud-bigquery==3.27.0
google-api-python-client==2.149.0
google-auth==2.35.0

# dev / test
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
respx==0.21.1
ruff==0.6.9
mypy==1.11.2
aiosqlite==0.20.0
# Offline PostgreSQL parser -- used by scripts/check_sql_syntax.py to
# parse every SQL constant in CI. No database connection involved.
pglast>=7
